name: Tests

# The platform ships no dependencies and has no build step, so the suite runs
# on Node's built-in test runner with nothing to install.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # npm test discovers every tests/**/*.test.mjs via scripts/run-tests.mjs,
      # so a new suite needs no change here and no Node glob support.
      - name: Run tests
        run: npm test

      - name: Check data scripts parse
        run: |
          for f in scripts/*.mjs; do
            node --check "$f"
          done
